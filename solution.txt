 Q. What is the difference between git stash apply and git stash pop?
 Ans:
  The git stash apply command leaves the topmost stash on the stash list so that we can later use it,
  on the other hand, the stash pop command in git removes or throw away the latest or the topmost stashed.